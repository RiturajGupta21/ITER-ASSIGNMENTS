/* This is a cooment*/
/*This one seems like a comment doesn't it*/


/*  Calculate and display the sum of two input values*/
#include <stdio.h>

int main(void) {
    int X, /*first input vale x,*/ x,/*second input value,*/sum /*sum of inputs*/;
    scanf("%d %d",&X,&x);
    sum=X+x;
    printf("%d + %d = %d \n",X,x,sum);
    return 0;
}
